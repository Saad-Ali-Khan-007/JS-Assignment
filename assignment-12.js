// Ask the user to provide their first and last name
// print their full name on the console (use template literals for concatenation)
const firstName = prompt("Enter your first name","Type Here")
const lastName = prompt("Enter your last name","Type Here")
const fullName = `${firstName} ${lastName}`
console.log(fullName)